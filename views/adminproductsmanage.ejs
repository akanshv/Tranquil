<%layout('./layouts/profile-boilerplate')%>

<link rel="stylesheet" href="/css/gradient.css">
<div class="wellness-div">
  <img src="https://i.imgur.com/uc9EeQL.png" class="wellness-banner" alt="wellness">
</div>


<section class="books section-p1" >
    <div class="text-center container py-5">
        <h1 class="mb-5 green2 product-type"><strong style="font-family: 'Roboto', sans-serif;">Mindfulness Books</strong></h1>
      </div>
    <div class="row">
      <% for( let product of prod ) { %>
     
        <% if (product.Type==='Mindfulness Books') { %>
          <div class="col-lg-4 col-md-6 mb-4">
            <div class="card">
              <div class="bg-image hover-zoom ripple" data-mdb-ripple-color="light">
                <img src="<%= product.image %>"
                  class="w-100" />
                <a href="#!">
                  <div class="hover-overlay">
                    <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                  </div>
                </a>
              </div>
              <div class="card-body">
                <a href="" class="text-reset">
                  <h5 class="card-title mb-3"><%= product.Name %></h5>
                </a>
                <a href="" class="text-reset">
                  <p><%= product.author %></p>
                </a>
                <h6 class="mb-3">MRP : ₹<%= product.Price%></h6>
                <form action="/admin/adminproductdelete/<%= product._id %>?_method=DELETE" method="post" id="<%= product._id %>_deleter"></form>
              <form action="/admin/adminproductupdate/<%= product._id %>?_method=PUT" method="post" id="<%= product._id %>_updater"></form>
                <label for="price">Discounted Price</label><br>
                <input type="Number" id="price" min="1" max="<%= product.Price %>" form="<%= product._id %>_updater" name="productcutprice" placeholder=" <%= product.Cutprice %>"><br>
                <label for="price">Stock</label><br>
                <input type="Number" id="price" min='0' name="productstock" form="<%= product._id %>_updater" placeholder=" <%= product.Stock %>"><br>
                <button class="btn btn-outline-primary mt-3" type="submit" form="<%= product._id %>_updater">Submit</button>
                <button class="btn btn-outline-danger mt-3" type="submit" form="<%= product._id %>_deleter">Delete</button>
             
                
               </div>
            </div>
      </div>
        <% } %>
      
      <% } %>
  
    </div>
    </div>
  </section>   


<section class="stress-busters section-p1" >
    <div class="text-center container py-5">
        <h1 class="mb-6 green2 product-type"><strong style="font-family: 'Roboto', sans-serif;">Stress Busters</strong></h1>
    </div>
    <div class="row">
      <% for( let product of prod ) { %>
     
        <% if (product.Type==='Stress Busters') { %>
          <div class="col-lg-4 col-md-6 mb-4">
            <div class="card">
              <div class="bg-image hover-zoom ripple" data-mdb-ripple-color="light">
                <img src="<%= product.image %>"
                  class="w-100" />
                <a href="#!">
                  <div class="hover-overlay">
                    <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                  </div>
                </a>
              </div>
              <div class="card-body">
                <a href="" class="text-reset">
                  <h5 class="card-title mb-3"><%= product.Name %></h5>
                </a>
                <a href="" class="text-reset">
                  <p><%= product.author %></p>
                </a>
                <h6 class="mb-3">
                  MRP : ₹<%= product.Price%>
                </h6>
                <form action="/admin/adminproductdelete/<%= product._id %>?_method=DELETE" method="post" id="<%= product._id %>_deleter"></form>
                <form action="/admin/adminproductupdate/<%= product._id %>?_method=PUT" method="post" id="<%= product._id %>_updater"></form>
                  <label for="price">Discounted Price</label><br>
                  <input type="Number" id="price" min="1" max="<%= product.Price %>" form="<%= product._id %>_updater" name="productcutprice" placeholder=" <%= product.Cutprice %>"><br>
                  <label for="price">Stock</label><br>
                  <input type="Number" id="price" min='0' name="productstock" form="<%= product._id %>_updater" placeholder=" <%= product.Stock %>"><br>
                  <button class="btn btn-outline-primary mt-3" type="submit" form="<%= product._id %>_updater">Submit</button>
                  <button class="btn btn-outline-danger mt-3" type="submit" form="<%= product._id %>_deleter">Delete</button>
    
                
               </div>
            </div>
      </div>
        <% } %>
      
      <% } %>
    </div>
  </div>
</section>

<section class="herbals section-p1" >
  <div class="text-center container py-5">
      <h1 class="mb-6 green2 product-type"><strong style="font-family: 'Roboto', sans-serif;">Herbals</strong></h1>
  </div>
  <div class="row">
    <% for( let product of prod ) { %>
     
      <% if (product.Type==='Herbals') { %>
        <div class="col-lg-4 col-md-6 mb-4">
          <div class="card">
            <div class="bg-image hover-zoom ripple" data-mdb-ripple-color="light">
              <img src="<%= product.image %>"
                class="w-100" />
              <a href="#!">
                <div class="hover-overlay">
                  <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                </div>
              </a>
            </div>
            <div class="card-body">
              <a href="" class="text-reset">
                <h5 class="card-title mb-3"><%= product.Name %></h5>
              </a>
              <a href="" class="text-reset">
                <p><%= product.author %></p>
              </a>
              <h6 class="mb-3">
                MRP : ₹<%= product.Price%>
              </h6>
              <form action="/admin/adminproductdelete/<%= product._id %>?_method=DELETE" method="post" id="<%= product._id %>_deleter"></form>
              <form action="/admin/adminproductupdate/<%= product._id %>?_method=PUT" method="post" id="<%= product._id %>_updater"></form>
                <label for="price">Discounted Price</label><br>
                <input type="Number" id="price" min="1" max="<%= product.Price %>" form="<%= product._id %>_updater" name="productcutprice" placeholder=" <%= product.Cutprice %>"><br>
                <label for="price">Stock</label><br>
                <input type="Number" id="price" min='0' name="productstock" form="<%= product._id %>_updater" placeholder=" <%= product.Stock %>"><br>
                <button class="btn btn-outline-primary mt-3" type="submit" form="<%= product._id %>_updater">Submit</button>
                <button class="btn btn-outline-danger mt-3" type="submit" form="<%= product._id %>_deleter">Delete</button>
             </div>
          </div>
    </div>
      <% } %>
    
    <% } %>
  </div>
</section>

<section class="sleepwell section-p1" >
  <div class="text-center container py-5">
      <h1 class="mb-6 green2 product-type"><strong style="font-family: 'Roboto', sans-serif;">Sleepwell</strong></h1>
  </div>
  <div class="row">
    <% for( let product of prod ) { %>
     
      <% if (product.Type==='Sleepwell') { %>
        <div class="col-lg-4 col-md-6 mb-4">
          <div class="card">
            <div class="bg-image hover-zoom ripple" data-mdb-ripple-color="light">
              <img src="<%= product.image %>"
                class="w-100" />
              <a href="#!">
                <div class="hover-overlay">
                  <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                </div>
              </a>
            </div>
            <div class="card-body">
              <a href="" class="text-reset">
                <h5 class="card-title mb-3"><%= product.Name %></h5>
              </a>
              <a href="" class="text-reset">
                <p><%= product.author %></p>
              </a>
              <h6 class="mb-3">
                MRP : ₹<%= product.Price%>
              </h6>
              <form action="/admin/adminproductdelete/<%= product._id %>?_method=DELETE" method="post" id="<%= product._id %>_deleter"></form>
              <form action="/admin/adminproductupdate/<%= product._id %>?_method=PUT" method="post" id="<%= product._id %>_updater"></form>
                <label for="price">Discounted Price</label><br>
                <input type="Number" id="price" min="1" max="<%= product.Price %>" form="<%= product._id %>_updater" name="productcutprice" placeholder=" <%= product.Cutprice %>"><br>
                <label for="price">Stock</label><br>
                <input type="Number" id="price" min='0' name="productstock" form="<%= product._id %>_updater" placeholder=" <%= product.Stock %>"><br>
                <button class="btn btn-outline-primary mt-3" type="submit" form="<%= product._id %>_updater">Submit</button>
                <button class="btn btn-outline-danger mt-3" type="submit" form="<%= product._id %>_deleter">Delete</button>
            
              
             </div>
          </div>
    </div>
      <% } %>
    
    <% } %>
</div>
<div class=" d-flex justify-content-left align-items-center px-5 mt-6">
    <button type="button"  onclick="window.location.href='/admin/adminprofile'" class="btn btn-primary btn-lg" >Back</button>
</div>
</section>


<!-- <script src="/js/products.js"></script> -->
